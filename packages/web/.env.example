# Redis Configuration for Web Streaming
# The web app subscribes to Redis pub/sub to receive conversation updates

# Redis server hostname
REDIS_HOST=localhost

# Redis server port
REDIS_PORT=6379

# Redis password (optional, leave empty if not required)
REDIS_PASSWORD=

# Redis pub/sub channel name (must match CLI app's WEB_STREAM_REDIS_CHANNEL)
REDIS_CHANNEL=ai-chat:stream
